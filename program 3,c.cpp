#include<stdio.h>
int main()
{
	int a,b,sum,diff;
	printf("enter a and b values");
	scanf("%d%d",&a,&b);
	sum=a+b;
	diff=a-b;
	printf("sum of digits is %d",sum);
	printf("diff of digits is %d",diff);
	return 0;
}
